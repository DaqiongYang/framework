jar{
    enabled = true
}

bootJar {
    enabled = true
}

dependencies {
    api project(':model')
    api project(':common')
    //swagger2
    implementation deps.swagger2
    implementation deps.swagger_ui
}